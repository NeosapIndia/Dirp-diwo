import { Routes } from '@angular/router';
import { ManageAccountComponent } from './manage-account.component';

export const ManageAccountRoutes: Routes = [
  { path: '', component: ManageAccountComponent }
];
