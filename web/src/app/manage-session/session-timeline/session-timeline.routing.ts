import { Routes } from '@angular/router';
import { SessionTimelineComponent } from './session-timeline.component';

export const SessionTimelineRoutes: Routes = [{ path: '', component: SessionTimelineComponent }];
