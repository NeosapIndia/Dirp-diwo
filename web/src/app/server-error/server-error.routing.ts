import { Routes } from '@angular/router';

import { ServerErrorComponent } from './server-error.component';

export const ServerErrorRoutes: Routes = [
  { path: '', component: ServerErrorComponent }
];
