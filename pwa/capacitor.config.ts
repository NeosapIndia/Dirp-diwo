import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mnt.babygq',
  appName: 'Babler',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
