import { Routes } from '@angular/router';
import { LandRoverComponent } from './land-rover.component';

export const LandRoverRoutes: Routes = [
	{ path: '', component: LandRoverComponent },
	// { path: 'add-or-edit-drip', component: AddEditPostComponent }
];
